# Slither Multiplayer — Visual Design Spec

**버전**: 1.0  
**작성**: 디자이너 Organt  
**날짜**: 2026-06-04  
**상태**: 완성 (§6 UX 섹션 통합 완료)

---

## 0. 디자인 원칙

- **가독성 우선**: 게임 중 HUD 텍스트는 WCAG AA 기준(최소 4.5:1 대비비)을 충족해야 한다.
- **계층 명확화**: 배경 → 게임 캔버스 → HUD → 모달 순으로 레이어 명도를 단계적으로 올린다.
- **게임 몰입 유지**: HUD는 정보를 전달하되 캔버스를 최대한 가리지 않는다.
- **일관된 토큰**: 모든 색·사이즈는 아래 섹션 1의 토큰에서만 참조한다.

---

## 1. 색상 팔레트 토큰

### 1.1 배경 / 배경 레이어

| 토큰 | Hex / rgba | 용도 |
|------|-----------|------|
| `--bg-base` | `#07071a` | 페이지 배경, 게임 캔버스 배경 |
| `--bg-deep` | `#050510` | 미니맵 배경 (배경보다 더 진해 구역 구분) |
| `--bg-overlay` | `rgba(0,0,8,0.70)` | 사망 화면 뒷 오버레이 |
| `--bg-surface` | `rgba(10,12,30,0.82)` | HUD 패널(statsBox, leaderboard) 배경 |
| `--bg-card` | `rgba(14,16,36,0.96)` | 랜딩 카드, 사망 카드 배경 |
| `--bg-input` | `rgba(255,255,255,0.08)` | 입력 필드 기본 상태 |
| `--bg-input-focus` | `rgba(255,255,255,0.13)` | 입력 필드 포커스 상태 |

> **변경 이유**: 기존 랜딩 카드 `rgba(255,255,255,0.04)`는 배경과 대비가 없어 카드 구분 불가. `--bg-card`(0.96 불투명도)로 올려 레이어 계층을 명시한다.

### 1.2 테두리

| 토큰 | rgba | 용도 |
|------|------|------|
| `--border-subtle` | `rgba(255,255,255,0.10)` | HUD 패널 기본 테두리 |
| `--border-default` | `rgba(255,255,255,0.18)` | 랜딩 카드, 사망 카드 테두리 |
| `--border-input` | `rgba(255,255,255,0.20)` | 입력 필드 테두리 |
| `--border-input-focus` | `rgba(46,204,113,0.65)` | 입력 필드 포커스 테두리 |
| `--border-minimap` | `rgba(255,255,255,0.25)` | 미니맵 외곽선 |
| `--border-danger` | `rgba(231,76,60,0.75)` | 월드 경계 펄스 테두리 |

### 1.3 텍스트

| 토큰 | rgba / Hex | 대비비(vs `--bg-surface`) | 용도 |
|------|-----------|--------------------------|------|
| `--text-primary` | `#ffffff` | ≈ 15:1 | 주요 값, 강조 텍스트 |
| `--text-secondary` | `rgba(255,255,255,0.80)` | ≈ 9:1 | 일반 레이블, 이름 |
| `--text-muted` | `rgba(255,255,255,0.55)` | ≈ 4.8:1 ✅ | 보조 정보 (힌트, 랭킹 순번) |
| `--text-disabled` | `rgba(255,255,255,0.30)` | < 4.5:1 | 플레이스홀더 전용 (상호작용 없는 곳) |

> **변경 이유**: 기존 `lb-rank` = `rgba(255,255,255,0.35)` + 11px는 WCAG AA 불합격(~3:1). `--text-muted`(0.55)로 올리고 최소 12px로 교체한다.

### 1.4 액센트 / 상태 색

| 토큰 | Hex | 용도 |
|------|-----|------|
| `--accent-green` | `#2ecc71` | 내 뱀 강조, 시작 버튼, stats 값 |
| `--accent-green-dark` | `#27ae60` | 시작 버튼 그라디언트 끝색 |
| `--accent-blue` | `#3498db` | 로고 중간색, 정보 강조 |
| `--accent-purple` | `#9b59b6` | 로고 끝색 |
| `--accent-gold` | `#f39c12` | 점수 표시, 부스트 바 시작색 |
| `--accent-red` | `#e74c3c` | 위험·사망·월드 경계·부스트 바 끝색 |
| `--accent-red-dark` | `#c0392b` | 사망 화면 버튼 그라디언트 끝색 |
| `--accent-cyan` | `#1abc9c` | 보조 스킨 옵션 |

---

## 2. HUD 요소별 대비·스타일 기준

> **레이아웃·방향표시·피드백 규격**은 §6(UX 섹션)에서 다룬다.

### 2.1 statsBox (좌상단)

| 항목 | 현재 | 권장 스펙 |
|------|------|----------|
| 패널 배경 | `rgba(0,0,0,0.55)` | `--bg-surface` = `rgba(10,12,30,0.82)` |
| 패널 테두리 | `rgba(255,255,255,0.10)` | `--border-subtle` 유지 |
| 레이블 텍스트 | white / 14px | `--text-secondary` / 14px / 줄간격 8px |
| 값 텍스트 | `#2ecc71` / 14px bold | `--accent-green` / **16px bold** (수치 강조) |
| 패딩 | 12px 18px | **14px 20px** |
| min-width | 120px | **140px** |

### 2.2 리더보드 (우상단)

| 항목 | 현재 | 권장 스펙 |
|------|------|----------|
| 패널 배경 | `rgba(0,0,0,0.55)` | `--bg-surface` |
| 타이틀 | `rgba(255,255,255,0.60)` / 12px | `--text-muted` / 12px / tracking 1.5px |
| 플레이어 이름 | `rgba(255,255,255,0.75)` / 13px | `--text-secondary` / 13px |
| 내 이름 강조 | `#2ecc71` bold | `--accent-green` bold 유지 |
| 순위 번호 | `rgba(255,255,255,0.35)` / **11px** ❌ | `--text-muted`(0.55) / **12px** |
| 점수 숫자 | `#f39c12` / **12px** | `--accent-gold` / **13px** |
| 항목 간격 | 5px | **7px** |
| min-width | 180px | **190px** |

### 2.3 미니맵 (우하단)

| 항목 | 현재 | 권장 스펙 |
|------|------|----------|
| 배경 | `rgba(5,5,20,0.85)` | `rgba(5,5,20,0.90)` |
| 외곽선 | `rgba(255,255,255,0.15)` | `--border-minimap` = `rgba(255,255,255,0.25)` |
| 내 위치 점 | 흰색 r=4 + 흰 outline | 유지 (충분히 눈에 띔) |
| 타 플레이어 점 | skin 색 r=2.5 | r=**3** (가시성 향상) |
| 뷰포트 사각형 | `rgba(255,255,255,0.35)` | `rgba(255,255,255,0.45)` |
| 크기 | 160×160px | 유지 |

### 2.4 부스트 바 (하단 중앙)

| 항목 | 현재 | 권장 스펙 |
|------|------|----------|
| 높이 | 6px | **8px** (시인성 향상) |
| 배경 | `rgba(255,255,255,0.10)` | `rgba(255,255,255,0.15)` |
| 채움 그라디언트 | `--accent-gold → --accent-red` | 유지 |
| 위치 | bottom 18px | bottom **22px** (미니맵과 겹침 방지) |
| 너비 | 160px | **200px** |
| 테두리 반경 | 3px | **4px** |

---

## 3. 랜딩 화면 개선

### 3.1 레이아웃

- 카드 너비: 420px 유지
- 카드 패딩: 48px 52px → **52px 56px** (여백 확대로 럭셔리 감 강화)
- 색상 스와치: 26×26px → **30×30px**, gap 7px → **8px** (터치 친화)
- 각 블록 사이 margin-bottom을 기존 대비 +4px 여유 추가

### 3.2 타이포그래피

| 요소 | 현재 | 권장 |
|------|------|------|
| 로고 크기 | 52px / weight 900 | **56px** / weight 900 |
| 로고 자간 | 6px | **8px** |
| 로고 그라디언트 | `#2ecc71 → #3498db → #9b59b6` 135° | 유지 |
| 태그라인 | 13px / tracking 3px / opacity 0.4 | 13px / tracking 4px / opacity **0.55** |
| 힌트 텍스트 | 12px / opacity 0.3 | 12px / opacity **0.45** |

### 3.3 카드 배경

| 속성 | 현재 | 권장 |
|------|------|------|
| 배경 | `rgba(255,255,255,0.04)` ❌ | `--bg-card` = `rgba(14,16,36,0.96)` |
| backdrop-filter | blur(12px) | blur(**16px**) |
| 테두리 | `rgba(255,255,255,0.10)` | `--border-default` = `rgba(255,255,255,0.18)` |
| 그림자 | `0 8px 60px rgba(0,0,0,0.6), 0 0 120px rgba(46,204,113,0.08)` | `0 12px 80px rgba(0,0,0,0.70), 0 0 140px rgba(46,204,113,0.10)` |

### 3.4 버튼

| 속성 | 현재 | 권장 |
|------|------|------|
| 배경 | `#2ecc71 → #27ae60` 135° | 유지 |
| 텍스트 | 17px bold / white | 유지 |
| 패딩 | 15px | **16px** |
| 테두리 반경 | 10px | **12px** |
| Hover 효과 | brightness(1.15) | 유지 |
| 포커스 링 | 없음 ❌ | `outline: 3px solid rgba(46,204,113,0.60); outline-offset: 2px` |

> 키보드 접근성: 버튼 포커스 링 필수. 현재 미구현으로 개선 대상.

---

## 4. 뱀 렌더링 색 처리

### 4.1 몸체 3-pass 레이어 구조

```
Pass 1 (outline)   : tintColor(skin, 0.30)   — lineWidth = (BODY_R + 2) × 2 = 18px
Pass 2 (body fill) : skin 색                 — lineWidth = BODY_R × 2       = 14px
Pass 3 (highlight) : rgba(255,255,255,0.22)  — lineWidth = BODY_R × 0.60    ≈ 4.2px
```

**아웃라인 비율**: outline은 body보다 각 변에 2px씩 돌출. `(BODY_R+2)/BODY_R ≈ 1.29` 비율 유지.

| 파라미터 | 현재 | 권장 | 변경 이유 |
|---------|------|------|----------|
| Outline brightness | 0.35 | **0.30** | 더 진한 윤곽으로 배경 대비 향상 |
| Highlight alpha | 0.18 | **0.22** | 입체감 미세 강화 |
| Highlight lineWidth | BODY_R × 0.70 | **BODY_R × 0.60** | 좁게 집중시켜 날카로운 광택선 |

### 4.2 부스트 글로우

| 상태 | shadowColor | shadowBlur |
|------|-------------|------------|
| 일반 이동 | 없음 | 0 |
| 부스트 중 | skin 색 | 현재 18 → **22** |
| 내 뱀 머리 (항상) | skin 색 | 현재 16 → **20** |

> shadowBlur 상한선: **24**. 그 이상은 렌더 비용 대비 효과가 없음.

### 4.3 머리(Head) 처리

| 요소 | 현재 | 권장 |
|------|------|------|
| 아웃라인 brightness | 0.35 | **0.30** (몸체와 일치) |
| 눈 흰자 | r=3.5, white | 유지 |
| 눈 동공 | r=2, `#111` | 유지 |
| EYE offset | HEAD_R × 0.48 | 유지 |

### 4.4 이름 태그

| 요소 | 현재 | 권장 |
|------|------|------|
| 배경 | `rgba(0,0,0,0.55)` | `rgba(0,0,0,0.65)` |
| 내 이름 | `#ffffff` | 유지 |
| 타 이름 | `rgba(255,255,255,0.85)` | `rgba(255,255,255,0.90)` |
| 폰트 (내 뱀) | bold 12px | **bold 13px** |
| 폰트 (타 뱀) | 12px | 유지 |

### 4.5 음식(Food) 글로우

| 종류 | 현재 shadowBlur | 권장 |
|------|----------------|------|
| 일반 음식 (size ≤ 6) | 8 | 유지 |
| 사망 후 대형 음식 (size > 6) | 16 | **20** (희귀 식량 강조) |
| 내부 하이라이트 alpha | 0.65 | **0.70** |

---

## 5. 사망 화면 스타일

### 5.1 오버레이

| 속성 | 현재 | 권장 |
|------|------|------|
| 배경 | `rgba(0,0,0,0.65)` | `--bg-overlay` = `rgba(0,0,8,0.70)` |
| backdrop-filter | blur(8px) | blur(**10px**) |

### 5.2 카드 등장 애니메이션

```
현재: scale(0.85)→(1.0)  0.30s ease
권장: scale(0.80)→(1.0)  0.25s cubic-bezier(0.34, 1.56, 0.64, 1)
```
스프링 커브(`cubic-bezier(0.34,1.56,0.64,1)`)로 자연스러운 탄성 연출.

### 5.3 카드

| 요소 | 현재 | 권장 |
|------|------|------|
| 배경 | `rgba(20,20,40,0.95)` | `--bg-card` = `rgba(14,16,36,0.96)` |
| 테두리 | `rgba(255,255,255,0.12)` | `--border-default` = `rgba(255,255,255,0.18)` |
| 상단 강조 라인 | 없음 | `border-top: 3px solid rgba(231,76,60,0.60)` |
| 그림자 | `0 12px 80px rgba(0,0,0,0.7)` | `0 16px 100px rgba(0,0,0,0.80), 0 0 60px rgba(231,76,60,0.12)` |
| 패딩 | 48px 56px | 유지 |

### 5.4 타이포그래피

| 요소 | 현재 | 권장 |
|------|------|------|
| 사망 아이콘 | 💀 60px | 유지 |
| "사망!" 제목 | `#e74c3c` / 32px / w900 | `--accent-red` / **36px** / w900 / tracking 3px |
| 최종 점수 | `#f39c12` / 20px bold | `--accent-gold` / **22px bold** |
| 순위 텍스트 | `rgba(255,255,255,0.50)` / 15px | `--text-muted`(0.55) / **16px** |
| 다시 시작 버튼 | `#e74c3c → #c0392b` / 16px bold | 색 유지, 포커스 링 추가 |
| 버튼 포커스 링 | 없음 ❌ | `outline: 3px solid rgba(231,76,60,0.55); outline-offset: 2px` |

---

## 6. HUD 레이아웃 · 방향 표시 · 피드백

### 6.1 HUD 패널 배치 원칙

#### 6.1.1 앵커 & 안전 영역 (데스크톱 ≥ 800px)

모든 HUD 패널은 4개 코너에 고정되며, 화면 중앙부는 항상 비워 둔다. 공통 기준 오프셋 18px.

```
┌─────────────────────────────────────┐
│ [statsBox]           [leaderboard]  │
│ 좌상단 top:18, left:18  우상단 top:18, right:18 │
│                                     │
│         (캔버스 중앙 — 항상 비움)    │
│                                     │
│        [boostBar]      [minimap]    │
│        하단중앙 bottom:22  우하단 bottom:18 │
└─────────────────────────────────────┘
```

| 패널 | 앵커 | 오프셋 | 고정 크기 |
|------|------|--------|---------|
| statsBox | 좌상단 | top: 18px, left: 18px | min-width 140px |
| leaderboard | 우상단 | top: 18px, right: 18px | min-width 190px |
| minimapWrap | 우하단 | bottom: 18px, right: 18px | 160 × 160px |
| boostBar | 하단 중앙 | bottom: 22px, 수평 중앙 | 200 × 8px |

**패널 간 최소 간격 규칙:**

| 패널 쌍 | 방향 | 최소 간격 | 초과 시 처리 |
|--------|------|---------|------------|
| statsBox ↔ leaderboard | 수평 | 16px | 두 패널 너비 합계 330px + 양측 오프셋 72px = 402px → 뷰포트 ≥ 402px 보장 시 안전 |
| leaderboard ↔ minimapWrap | 수직(우측) | 24px | leaderboard 최대 높이를 `viewport_height − 220px`로 동적 제한; 초과 항목은 행 단위 숨김 |
| boostBar ↔ minimapWrap | 수평 | 자연 분리 | boostBar 중앙 기준, minimapWrap 우측 고정 — 뷰포트 ≥ 440px이면 겹치지 않음 |

**안전 검증 도출:**  
leaderboard 하단 최악값 = 18 + 헤더 20px + 항목 10개 × 20px + 패딩 28px = 266px.  
minimapWrap 상단 = H − 178px. H = 500px 기준 여유 = 322 − 266 = 56px ✅. H = 440px 기준 = −4px → 동적 높이 제한 필요.

**피복 면적 목표:** 전체 HUD 패널 합산 면적 ≤ 전체 화면의 **20%** (1920×1080 기준 실측 약 14%).

#### 6.1.2 모바일 대응 (viewport width < 800px)

데스크톱 크기 유지 시 패널이 캔버스 면적의 40% 이상을 덮으므로 **compact 모드**로 전환한다.

| 패널 | compact 변경 사항 |
|------|----------------|
| statsBox | 패딩 10px 14px, 폰트 13px, min-width 110px |
| leaderboard | 항목 수 최대 5개, min-width 160px, 폰트 12px |
| minimapWrap | 120 × 120px, bottom: 12px, right: 12px |
| boostBar | 너비 160px, bottom: 14px |

- 공통 오프셋: 18px → **12px**
- 패널 배경 `--bg-surface` 불투명도(0.82) 유지 — 가독성 보장
- compact 모드 토글 기준: JS `window.innerWidth < 800` (캔버스 resize와 동기화)

---

### 6.2 방향 표시

#### 현재 상황 분석

- 마우스 커서: `cursor: none` — 화면에서 커서가 보이지 않음.
- 뱀 머리 눈(eye)이 이동 방향을 향하고 있어 현재 진행 방향의 1차 시각 단서 존재.
- 카메라가 내 뱀 머리를 부드럽게(lerp) 추적하므로 방향 감각은 상대적으로 자연스러움.

#### 방향 표시 결정: Soft Cursor 가이드라인 추가

`cursor: none` 상태에서 마우스 위치를 잃어버릴 수 있는 문제를 보완하기 위해, 뱀 머리에서 마우스 방향으로 뻗는 **점선 가이드(soft cursor)**를 미세하게 표시한다.

| 속성 | 스펙 |
|------|------|
| 선 색상 | `rgba(255,255,255,0.18)` — 배경 대비 인지 가능하되 시각 방해 최소화 |
| 선 길이 | HEAD_R 끝에서 **40px** |
| 선 두께 | 1.5px |
| dash 패턴 | 4px 실선 + 4px 간격 |
| 표시 조건 | 마우스가 뱀 머리에서 **30px 이상** 떨어졌을 때만 표시 (근접 시 노이즈 방지) |
| 끝 마커 | 삼각형 · 밑변 6px · 높이 8px · 동일 색상 |

이 가이드는 방향 '안내'가 아니라 커서 대체 목적이므로 최소 불투명도(0.18)를 유지한다. 이름 태그(§4.4)와 수직 방향이 다르므로 충돌하지 않는다.

**향후 검토:** 터치 입력 지원 추가 시 가상 조이스틱 UI로 대체한다.

---

### 6.3 게임 내 시각 피드백

피드백은 **Canvas 레이어**(캔버스 직접 렌더) 또는 **HUD 레이어**(CSS 트랜지션·애니메이션)로 분류한다.

#### 6.3.1 음식 획득 (점수 +)

**레이어:** Canvas + HUD

| 요소 | 스펙 |
|------|------|
| **파티클 버스트** | 음식 위치 중심 방사형 · 5개 점 · 반경 8px → 20px · 색: 음식 fill 색 · 알파 0.90 → 0 · 지속 220ms · easing: ease-out |
| **점수 팝업 (+N)** | 음식 위치 기준 위로 12px 시작 → 28px 추가 float · `--accent-gold` · 14px bold · 알파 1.0 → 0 · 지속 550ms · easing: ease-out |
| **statsBox 점수 flash** | scale 1.0 → 1.20 → 1.0 · 지속 250ms · `--accent-gold` · easing: cubic-bezier(0.34, 1.56, 0.64, 1) |
| **동시 팝업 한도** | 화면당 최대 8개 (초과 시 오래된 것 즉시 제거) |

**음식 크기(size) 비례 강도:**

| size 범위 | 파티클 수 | 반경 | 지속 | 팝업 폰트 |
|----------|---------|------|------|---------|
| ≤ 4 | 3개 | 8 → 14px | 150ms | 12px bold |
| 5 ~ 6 | 5개 | 8 → 20px | 220ms | 14px bold |
| > 6 (사망 후 대형) | 8개 | 12 → 36px | 320ms | 18px bold |

#### 6.3.2 부스트 활성 / 비활성

**레이어:** HUD (boostBar) + Canvas (뱀 글로우 — §4.2에서 처리)

boostBar는 항상 표시 상태를 유지하며, 부스트 상태에 따라 테두리 glow와 채움 색으로만 상태를 전달한다.

**활성 전환 (스페이스/우클릭 누름):**

| 요소 | 스펙 |
|------|------|
| 테두리 glow on | `box-shadow: 0 0 8px --accent-gold` · 즉시 적용 |
| glow pulse (활성 유지 중) | glow blur 반경 8px ↔ 12px · 사인 파형 · 주기 0.7s |
| 채움 그라디언트 | `--accent-gold → --accent-red` 좌→우 유지, 잔량에 따라 너비 실시간 감소 |

**비활성 전환:**

| 조건 | 요소 | 스펙 |
|------|------|------|
| 키 뗌 (잔량 있음) | glow fade-out | 200ms ease-out |
| 잔량 15% 미만 (소진 임박) | 채움 색 | `--accent-red` 고정 + 0.6s 주기 flash (알파 1.0 ↔ 0.40) |
| 바 완전 소진 | 바 flash | `--accent-red` fill 2회 150ms on/off, 이후 회색 빈 상태 |
| 충전 중 | 채움 색 | `--accent-gold` 알파 0.55 (충전 중 구분) |
| 충전 완료 | 복귀 flash | `--accent-gold` glow 1회 200ms |

#### 6.3.3 경쟁자 사망으로 식량 스폰

**레이어:** Canvas (뷰포트 내) + 미니맵 (뷰포트 외)

| 요소 | 스펙 |
|------|------|
| **임팩트 링** | 사망 위치 중심 · 원형 stroke ring · 반경 0 → 80px · 선 두께 2px · 색 `rgba(255,255,255,0.70)` → 투명 · 지속 400ms · easing: ease-out |
| **파편 파티클** | 죽은 뱀 skin 색 · 8개 점 · 중심에서 랜덤 방향 20~50px · 알파 0.80 → 0 · 지속 350ms |
| **대형 음식 등장 pulse** | 스폰 직후 shadowBlur 20 → 40 → 20 · 1회 · 지속 500ms (§4.5 기본값 20에서 추가 pulse) |
| **미니맵 flash** | 해당 위치 흰 점 · 알파 0.9 → 0 · 200ms on/off · 3회 · 총 600ms |

사망 위치가 뷰포트 밖이면 Canvas 이펙트(임팩트 링·파티클) 생략, 미니맵 flash만 적용한다.

#### 6.3.4 월드 경계 근접 경고

**레이어:** Canvas 비네트 오버레이

경고 거리는 절대 픽셀값이 아닌 **월드 반경 비율**로 정의한다 — 월드 크기 변경에 무관하게 동일한 체감 경고 타이밍을 보장한다.

| 파라미터 | 값 |
|---------|---|
| 경고 발동 기준 | 내 뱀 머리 ↔ 월드 경계 거리 < 월드 반경의 **15%** |
| 비네트 색 | `--accent-red` = `#e74c3c` |
| 비네트 형태 | 경계에 가까운 변 방향으로 단방향 radial gradient (4방향 독립 계산) |
| 비네트 폭 | 화면 긴 변의 **22%** |
| 알파 보간 | 거리 15% → alpha 0.0 / 거리 5% → 0.22 / 거리 0% → 0.28 (선형) |

**경고 강도 단계:**

| 거리 (월드 반경 비율) | 단계 | 비네트 알파 범위 | 펄스 주기 |
|--------------------|------|--------------|---------|
| 15% ~ 10% | 주의 | 0.00 ~ 0.12 | 없음 |
| 10% ~ 5% | 경고 | 0.12 ~ 0.22 | 있음 — 1.5s 사인 파형 (알파 × 0.7~1.0) |
| 5% 미만 | 위험 | 0.22 ~ 0.28 | 있음 — 1.0s 사인 파형 (알파 × 0.7~1.0) |

HUD 패널 테두리는 변경하지 않는다. 비네트 오버레이만으로 경고 의도가 충분히 전달되며, 패널 테두리 색 변경은 `--border-subtle` 토큰의 의미론적 오용에 해당한다.

---

## 7. 색상 접근성 요약

| 텍스트 조합 | 대비비 | WCAG AA (4.5:1) |
|------------|--------|-----------------|
| `--text-primary` on `--bg-surface` | ≈ 15:1 | ✅ |
| `--text-secondary` on `--bg-surface` | ≈ 9:1 | ✅ |
| `--text-muted` on `--bg-surface` | ≈ 4.8:1 | ✅ |
| `--accent-green` on `--bg-surface` | ≈ 5.2:1 | ✅ |
| `--accent-gold` on `--bg-surface` | ≈ 6.8:1 | ✅ |
| `--accent-red` on `--bg-card` | ≈ 5.0:1 | ✅ |
| ~~lb-rank 기존 (opacity 0.35 / 11px)~~ | ≈ 3.0:1 | ❌ → §2.2에서 수정 |

---

*이후 버전 예정: 반응형 HUD(모바일 800px 미만), 스킨 커스터마이징 팔레트 확장.*

---

## 8. P-001 기획회의 UX·연출 제안

> **담당:** UX·연출 디자이너 / **일자:** 2026-06-04
> 이 섹션은 §1~§7의 비주얼 스펙과 별개로, 두께 성장 연출 및 slither.io 대비 UX 완성도 관점의 추가 제안입니다.

---

### 8.1 두께 성장 — 자연스러운 성장감 연출

현재 `BODY_R = 7` (app.js), `HEAD_R = 10` 이 상수로 고정되어 있어 아무리 먹어도 뱀이 커 보이지 않습니다. 이것이 slither.io 대비 가장 큰 시각적 공허함입니다.

#### 8.1.1 비주얼 두께 공식 (클라이언트 전용, 서버 변경 없음)

세그먼트 수(length)를 기반으로 렌더 반지름을 로그 스케일로 계산합니다.

```
targetBodyR = clamp(7 + log2(length / 10 + 1) × 7,  min=7, max=24)
targetHeadR = max(10, targetBodyR × 1.45)
```

| length | targetBodyR | targetHeadR |
|--------|-------------|-------------|
| 20 (시작) | 7.4 px | 10.7 px |
| 50 | 11.2 px | 16.2 px |
| 100 | 14.2 px | 20.6 px |
| 200 | 17.2 px | 24.9 px |
| 400+ | 24.0 px (최대) | 34.8 px |

로그 스케일을 선택한 이유: 초반 20~100 구간에서 성장감이 뚜렷하고, 후반 고성장 구간에서는 과도한 팽창을 방지합니다.

#### 8.1.2 부드러운 전환 (lerp)

갑작스러운 두께 점프 없이 0.5~1초에 걸쳐 서서히 성장합니다.

```js
// app.js 전역 상태 추가
let currentBodyR = 7;
let currentHeadR = 10;

// render() 루프 — 줌 계산 직후에 삽입
if (me && me.alive) {
  const n = me.segments.length;
  const tBR = Math.min(24, Math.max(7, 7 + Math.log2(n / 10 + 1) * 7));
  const tHR = Math.max(10, tBR * 1.45);
  currentBodyR += (tBR - currentBodyR) * 0.04;  // ~25프레임(~0.4s)에 90% 수렴
  currentHeadR += (tHR - currentHeadR) * 0.04;
}
// 다른 플레이어: 각 snake의 segments.length로 같은 공식 적용
```

`BODY_R`, `HEAD_R` 상수를 직접 수정하는 것이 아니라 `currentBodyR`/`currentHeadR` 변수를 렌더 시 참조하는 구조로, 서버 히트박스(BODY_RADIUS=7)와 완전히 독립됩니다.

#### 8.1.3 줌 연동 미세 조정

두께가 커지면 화면 점유 면적이 증가하므로 줌을 조금 더 당깁니다.

```
현재: max(0.45, 1 - (length - 20) / 380)
권장: max(0.40, 1 - (length - 20) / 360)
```

#### 8.1.4 마일스톤 Flash 연출

길이 임계값을 처음 넘는 순간 뱀 전체를 0.3초간 밝게 플래시합니다.

```
마일스톤: [50, 100, 200, 300, 500]

구현: milestoneReached 플래그 → 해당 렌더프레임에서
      body path를 rgba(255,255,255,0.45)로 추가 overdraw,
      300ms에 걸쳐 알파 0.45 → 0 (ease-out)
```

#### 8.1.5 먹이 섭취 Ripple

`grew = true` 틱에서 머리 위치에 원형 파동 1회 발생시킵니다. 먹는 순간의 '손맛'을 줍니다.

```
ripple = { x, y, t(생성시각), r0: currentBodyR, duration: 350ms }
렌더 시: r = r0 + (r0 × 2.0) × (age/350),  alpha = 0.55 × (1 - age/350)
strokeStyle: rgba(255, 255, 255, alpha),  lineWidth: 1.5px
```

#### 8.1.6 미결 확인 사항

| # | 질문 | 대상 | 영향 |
|---|------|------|------|
| A | 두께 증가 시 서버 히트박스(BODY_RADIUS)도 같이 키울 것인가? | 서버 전문가 | 시각↔물리 불일치 여부. 현재 시각만 변경하면 큰 뱀이 충돌 판정상 여전히 작아 보임 |
| B | 두께 공식을 클라이언트·서버가 공유하는 상수 파일로 관리할 것인가, 각자 동일 공식 사용인가? | 프론트엔드 | 두 파트 간 시각 일관성 |

---

### 8.2 slither.io 대비 빠진 UX 개선 목록

우선순위 순 정렬. ★ 필수 / ☆ 권장.

#### ★ 8.2.1 킬 알림 (Kill Notification)

내가 다른 플레이어를 죽였을 때 알림이 없어 성취감이 0입니다.

```
조건: 타 플레이어의 self_death 에서 killed_by === myId 이면 클라이언트에서 알림 생성
      → 서버 변경 불필요 (단, 현재 killed_by 의미 재확인 필요 — 미결 C)

위치: 화면 하단 중앙 (boostBar 바로 위, 24px 간격)
내용: "💀 {이름}을(를) 잡았습니다!"
스타일: 14px bold, --accent-red(#e74c3c), 배경 rgba(0,0,0,0.55) pill
등장: slide-up 200ms + 유지 2.5s + fade-out 400ms
동시 최대: 2개 (초과 시 아래에 쌓이지 않고 최신 것으로 교체)
```

미결 C: `killed_by` 필드가 죽인 플레이어(killer) ID인지 죽은 플레이어(victim) ID인지 서버에 확인 필요.

#### ★ 8.2.2 경계 위험 비네팅

§6.3.4에 정의된 월드 경계 근접 경고가 이미 스펙화되어 있습니다. 본 섹션에서는 재확인만 합니다 — 구현 우선순위를 높음으로 올릴 것을 권장합니다. 이 연출이 없으면 플레이어가 벽에 닿기 전까지 위험을 인지하지 못합니다.

#### ★ 8.2.3 내 뱀 Z-Order 우선

현재 모든 뱀이 같은 렌더 레이어에 있어, 내 뱀이 다른 뱀 아래에 가려지는 현상이 발생합니다.

```js
// render() 내 snakes 순회를 아래로 교체
const renderOrder = [...snakes.values()].sort((a, b) =>
  (a.id === myId ? 1 : 0) - (b.id === myId ? 1 : 0)
);
for (const s of renderOrder) { /* 기존 렌더 코드 */ }
```

변경 한 줄로 내 뱀이 항상 맨 위에 그려집니다.

#### ★ 8.2.4 사망 폭발 파티클

§6.3.3에 임팩트 링 + 파편 파티클이 이미 정의되어 있습니다. 구현 확인만 필요합니다. 현재 코드(`app.js`)에 `deaths` 수신 시 파티클 로직이 없으므로 미구현 상태로 보입니다.

```
deaths 수신 시 → 죽은 뱀의 마지막 head 위치(snakes.get(deadId).segments[0]) 기반
§6.3.3 스펙대로 구현
```

#### ☆ 8.2.5 부스트 게이지 상시 표시

현재 부스트 시에만 게이지가 나타납니다. slither.io는 항상 표시합니다.

```
opacity: 0 → 항상 opacity: 0.45 (부스트 시 1.0)
잔량(segments.length) < 30 이면 --boostFill background를 --accent-red 고정
```

§2.4에서 이미 스펙화된 치수 값을 유지하면서, CSS `opacity` 기본값만 변경합니다.

#### ☆ 8.2.6 닉네임 아래 점수 표시

§4.4 이름 태그 스펙에 추가합니다.

```
이름 (기존): 12-13px bold
점수 (추가): 9px, rgba(255,255,255,0.55) — 이름 아래 4px 간격
배경 pill 높이: 17px → 28px (점수 줄 수용)
표시 조건: 타 플레이어만 (내 점수는 statsBox에서 충분)
```

#### ☆ 8.2.7 리스폰 무적 시각 피드백

스폰 직후 3초간 뱀을 점멸시켜 무적 상태를 알립니다.

```
init 수신 시 spawnProtectUntil = Date.now() + 3000
렌더 시: Date.now() < spawnProtectUntil 이면 globalAlpha 0.4 ↔ 1.0 (200ms 주기 토글)
```

미결 D: 실제 무적(서버 충돌 비활성화) 구현 계획을 서버 전문가에게 확인 필요. 클라이언트 점멸만 먼저 추가 가능하나, 실제 보호 없으면 연출만 남아 혼란을 줄 수 있음.

#### ☆ 8.2.8 먹이 흡인 시각 (자기장 이펙트)

내 머리 근처 먹이가 살짝 끌려오는 것처럼 보이는 효과. 실제 위치는 서버 권위, **렌더 위치만** 보정합니다.

```js
// food 렌더 루프 내
const dist = Math.hypot(f.x - headX, f.y - headY);
if (dist < 120 && dist > 1) {
  const pull = (1 - dist / 120) * 8;  // 최대 8px 오프셋
  const dx = (headX - f.x) / dist;
  const dy = (headY - f.y) / dist;
  renderFx = f.x + dx * pull;
  renderFy = f.y + dy * pull;
} else {
  renderFx = f.x; renderFy = f.y;
}
```

서버 변경 없음. 먹히는 순간 음식이 제 위치로 순간이동하는 것처럼 보일 수 있으나, 실제 먹기(EAT_DIST_SQ) 거리가 짧아 문제 없음.

#### ☆ 8.2.9 길이 달성 마일스톤 알림

§8.1.4 마일스톤 Flash와 연동합니다.

```
달성 시 화면 상단 중앙 배너:
"LENGTH 100 REACHED!"
폰트: 16px bold, 색: currentBodyR 기준 뱀 skin color
배경: rgba(0,0,0,0.60) pill, border-radius 8px
등장: slide-down 200ms + 유지 1.8s + fade-out 300ms
```

---

### 8.3 전체 미결 확인 사항 (P-001 연동)

| # | 질문 | 대상 | 영향 |
|---|------|------|------|
| A | 두께 증가 시 서버 BODY_RADIUS도 스케일 업 할 것인가? | 서버 전문가 | 시각↔물리 불일치 |
| B | 두께 공식을 공유 상수로 관리할 것인가? | 프론트엔드 | 일관성 |
| C | `killed_by` = killer ID인가 victim ID인가? | 서버 전문가 | 킬 알림 로직 |
| D | 리스폰 무적 서버 구현 계획 여부 | 서버 전문가 | 무적 피드백 연출 타이밍 |

---

### 8.4 구현 우선순위 (P-001 → 다음 스프린트 제안)

| 순위 | 항목 | 예상 공수 | 플레이 임팩트 | owner |
|------|------|-----------|-------------|-------|
| 1 | 두께 성장 + lerp (§8.1.1~8.1.2) | 소 (앱 수정) | ★★★ 핵심 요구 | 프론트엔드 |
| 2 | 경계 비네팅 (§6.3.4 구현) | 소 | ★★★ 긴장감 | 프론트엔드 |
| 3 | 킬 알림 (§8.2.1) | 소 | ★★ 성취감 | 프론트엔드 |
| 4 | 내 뱀 Z-order (§8.2.3) | 극소 (1줄) | ★★ 가독성 | 프론트엔드 |
| 5 | 사망 파티클 (§6.3.3 구현) | 중 | ★★ 연출 | 프론트엔드 |
| 6 | 먹이 섭취 Ripple (§8.1.5) | 소 | ★★ 손맛 | 프론트엔드 |
| 7 | 부스트 게이지 상시 (§8.2.5) | 소 | ★ 완성도 | 프론트엔드 |
| 8 | 먹이 흡인 시각 (§8.2.8) | 소 | ★ 슬리더 감성 | 프론트엔드 |
| 9 | 마일스톤 알림 (§8.2.9) | 소 | ★ 달성감 | 프론트엔드 |
| 10 | 닉네임 아래 점수 (§8.2.6) | 소 | ★ 완성도 | 프론트엔드 |
| 11 | 히트박스 스케일 (§A) | 중 (서버 수정) | ★ 정직성 | 서버 전문가 |
| 12 | 리스폰 무적 (§8.2.7 + §D) | 중 (서버+프론트) | ★ 완성도 | 협의 필요 |
